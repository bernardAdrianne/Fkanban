import { FilterByColumnPipe } from './filter-by-column.pipe';

describe('FilterByColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
